#Gradle
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -XX:+UseParallelGC
org.gradle.parallel=true
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.configuration-cache=true

#Kotlin
kotlin.code.style=official
kotlin.mpp.stability.nowarn=true
kotlin.mpp.enableCInteropCommonization=true
kotlin.apple.xcodeCompatibility.nowarn=true

#Dokka (v2 generator — aggregated API site at build/dokka/html)
org.jetbrains.dokka.experimental.gradle.pluginMode=V2Enabled
org.jetbrains.dokka.experimental.gradle.pluginMode.noWarn=true

#Library coordinates
GROUP=io.github.yuroyami
VERSION=0.0.1
# — is an em-dash: gradle.properties is read as ISO-8859-1, so a raw UTF-8 dash here would mojibake in the POM.
DESCRIPTION=KiteCodec \u2014 Kotlin Multiplatform codec library. FFmpeg bindings today, platform-native codec stacks (MediaCodec, VideoToolbox, WebCodecs) on the roadmap.

#FFmpeg paths — overridden by buildSrc per target; this is the macOS dev default.
kitecodec.macos.homebrew.prefix=/opt/homebrew


kotlin.native.ignoreDisabledTargets=true